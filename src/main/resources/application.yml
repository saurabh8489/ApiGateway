eureka:
    client:
        fetch-registry: false
        register-with-eureka: false
    dashboard:
        path: /dashboard
    instance:
        hostname: localhost
server:
    port: 8761
spring:
    application:
        name: api-gateway
